---
layout: base.njk
permalink: '/sobre/'
title: 'Sobre'
description: 'Ajudo organizações a tornarem seus produtos digitais, processos e cultura mais acessíveis e usáveis.'
---

<section class="region">
  <div class="wrapper">
    <div class="flow prose">
      <h1>{{ 'pages.about.title' | i18n(locale) }}</h1>
      <p class="lede">{{ 'pages.about.lead' | i18n(locale) }}</p>

      <h2>{{ 'pages.about.journeyTitle' | i18n(locale) }}</h2>
      <p>{{ 'pages.about.journeyP1' | i18n(locale) }}</p>
      <p>{{ 'pages.about.journeyP2' | i18n(locale) }}</p>

      <h2>{{ 'pages.about.valuesTitle' | i18n(locale) }}</h2>
      <ul>
        {% for value in 'pages.about.values' | i18n(locale) %}
        <li>{{ value }}</li>
        {% endfor %}
      </ul>
    </div>
  </div>
</section>
